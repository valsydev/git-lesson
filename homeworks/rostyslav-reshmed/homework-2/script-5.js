const num = +prompt ("Введіть число, щоб перевірити, чи воно входить в діапазон від 0 до 500");

if ( num >= 0 && num <= 500) {
    alert ("Ваше число входить в діапазон від 0 до 500");
} else {
    alert ("Ваше число НЕ входить в діапазон від 0 до 500");
}
