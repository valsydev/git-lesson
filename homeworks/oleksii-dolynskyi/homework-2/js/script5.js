const rangeNumber = parseInt(prompt("введите число"));
if ( rangeNumber > 0 && rangeNumber <= 500) {
   alert('число входит в диапазон от 0 до 500');
} else {
    alert('число не входит в диапазон от 0 до 500');
}