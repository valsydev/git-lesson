const firstOperand = +prompt("Enter the first number","");
const secondOperand = +prompt("Enter the second number","");

const sum = firstOperand + secondOperand;
alert(sum);

const difference = firstOperand - secondOperand;
alert(difference);

const product = firstOperand * secondOperand;
alert(product);

const quotient = firstOperand / secondOperand;
alert(quotient);